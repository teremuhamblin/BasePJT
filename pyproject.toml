[project]
name = "BasePJT"
version = "1.0"
description = "Package Python militaire fictif pour LegionOps Tracker"
authors = [
    { name="Major Hamblin", email="example@example.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["apps"]

[tool.setuptools.package-data]
apps = ["*"]

[tool.setuptools.dynamic]
version = {attr = "apps.__version__"}

[project.urls]
Homepage = "https://github.com/teremuhamblin/BasePJT"
